(function() {
  'use strict';
  angular
    .module('app.frontup', [
      // libs

      // own
      'app.frontup.home',
      'app.menuBar',
      'app.accordion',
      'app.projectTable'
    ]);
})();
